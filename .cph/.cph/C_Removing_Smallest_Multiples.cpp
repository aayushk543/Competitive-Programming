#include<bits/stdc++.h>
using namespace std;
#define ll as long long

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;

        string s;
        cin >> s;

        ll ans = 0;

        for(int i = 0; i < s.length(); i++){
            
        }

        cout << ans << '\n';
    }
    return 0;
}
